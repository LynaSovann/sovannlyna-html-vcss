apiVersion: v1
kind: Service
metadata:
  name: sovannlyna-html-vcss-service
spec:
  type: NodePort
  selector:
    app: sovannlyna-html-vcss
  ports:
    - protocol: TCP
      port: 8344
      targetPort: 80
